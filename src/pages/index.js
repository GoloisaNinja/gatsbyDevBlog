import React from "react";
import { Headline, Layout, ArticleCardGrid } from "components";

export default function IndexPage() {
  return (
    <Layout>
      <Headline />
      <ArticleCardGrid />
    </Layout>
  );
}
